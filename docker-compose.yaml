services:
  cron-manager:
    image: mahelbir/cron-manager:latest
    container_name: cron-manager
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - ./data:/app/server/src/storage
